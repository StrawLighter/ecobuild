# Scripts

Utility and automation scripts for EcoBuild.

| Script | Purpose |
|--------|---------|
| *(coming soon)* | Local validator bootstrap, deploy helpers, seed data |

## Planned
- `bootstrap-validator.sh` — spin up a local Solana validator + deploy the Anchor program
- `seed-submissions.sh` — populate mock PoC submissions for verifier testing
- `deploy.sh` — deploy program to devnet/mainnet
